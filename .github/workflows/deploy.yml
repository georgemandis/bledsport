name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest and restart
        run: |
          cd /home/recurse/bledsport
          git pull origin main
          sudo systemctl restart bledsport.service
